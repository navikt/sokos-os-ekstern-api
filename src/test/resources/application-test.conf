ktor {
    environment = test
}

configuration {
    naisAppName = "sokos-os-ekstern-api"
    useAuthentication = true
}

# azureAdConfiguration er konfigurert i hver enkelt testfil

osConfiguration {
    endpointUrl = ""
    trustStore = "nav_truststore_path"
    trustStorePassword = "nav_truststore_password"
}